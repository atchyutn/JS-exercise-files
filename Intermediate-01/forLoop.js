// we'll be looping through an array of elements and render them using for loop

let days = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']

for (let index = 0; index < days.length; index++) {
  console.log(days[index]);
}